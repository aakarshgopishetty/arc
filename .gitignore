# Vercel
.vercel

# Environment variables (never commit real secrets)
.env
.env.local
.env*.local

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Node / Capacitor
node_modules/

# Capacitor build outputs — regenerated from index.html/manifest.json/sw.js/icons
# via `npm install && cap add android && cap sync android`, no need to version them
www/
android/